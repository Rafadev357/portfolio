import initTiltAnimation from './scripts/tilAnimation';
import initScrollReveal from "./scripts/scrollReveal";
import { targetElements, defaultProps } from "./data/scrollRevealConfij";

initScrollReveal({ targetElements, defaultProps});
initTiltAnimation();